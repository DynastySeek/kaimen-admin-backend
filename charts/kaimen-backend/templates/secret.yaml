apiVersion: v1
kind: Secret
metadata:
  name: {{ include "kaimen-backend.name" . }}-secret
  labels:
    {{- include "kaimen-backend.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.secrets }}
  {{ $key }}: "{{ $value }}"
  {{- end }}
