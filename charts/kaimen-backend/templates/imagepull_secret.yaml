apiVersion: v1
kind: Secret
metadata:
  name: dockersecret
  labels:
    {{- include "kaimen-backend.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.imagepullSecrets }}
  {{ $key }}: "{{ $value }}"
  {{- end }}
