# 简化版 FastAPI 服务配置
apiVersion: v1
kind: Service
metadata:
  name: jianbao-backend
  namespace: jianbao-system
  labels:
    app: jianbao-backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: jianbao-backend